document.addEventListener("DOMContentLoaded", () => {
  // Squelette frontend: aucun comportement implémenté
  // Ajoutez JointJS et les appels API ici plus tard
  // Exemples d’éléments DOM existants:
  const canvas = document.getElementById("canvas");
  if (canvas) {
    canvas.textContent = "Espace graphique (placeholder)";
  }
});


